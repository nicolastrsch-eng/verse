name = "verse-hooktheory"
main = "hooktheory-proxy.js"
compatibility_date = "2024-11-01"

# Secrets (set with `wrangler secret put <NAME>`, do NOT commit values):
#   HOOKTHEORY_USERNAME
#   HOOKTHEORY_PASSWORD
#   ALLOWED_ORIGIN   (optional, e.g. https://nicolastrsch-eng.github.io)
